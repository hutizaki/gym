{
  "name": "gymtrack",
  "version": "1.0.0",
  "description": "Workout tracking app",
  "scripts": {
    "dev": "live-server --port=3000 --no-browser",
    "start": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
} 